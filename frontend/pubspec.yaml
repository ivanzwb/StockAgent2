name: stock_agent
description: AI驱动的A股投资分析助手
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  web_socket_channel: ^3.0.0
  provider: ^6.1.0
  shared_preferences: ^2.3.0
  intl: ^0.20.2
  fl_chart: ^1.1.1
  flutter_markdown: ^0.7.0
  go_router: ^17.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

